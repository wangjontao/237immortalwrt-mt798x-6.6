name: RAX3000M NAND 23.05 DulWiFi

on:
  workflow_dispatch:
  push:
    branches: [rax3000m-nand-23.05-baseline]
    paths: ['.github/workflows/RAX3000M-NAND-23.05.yml']

permissions:
  contents: write

concurrency:
  group: rax3000m-nand-23.05-baseline
  cancel-in-progress: true

env:
  DEVICE: cmcc_rax3000m
  CCACHE_DIR: ${{ github.workspace }}/.ccache
  PASSWALL_TAG: 26.3.6-1
  PASSWALL2_TAG: 26.3.5-1
  PASSWALL_PACKAGES_COMMIT: 3e11c458c552aefd348232c627fd1ed9f8f08e41
  HOME_PROXY_COMMIT: edece28a0085f36d469ec82c8d45f562f602db53

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: Prepare runner
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL /opt/hostedtoolcache/Ruby
          sudo docker system prune -af || true
          sudo apt-get update
          sudo apt-get install -y build-essential ccache clang flex bison g++ gawk gcc-multilib gettext git libncurses-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev file wget curl jq openssl zstd
      - uses: actions/setup-go@v5
        with:
          go-version: 1.24.x
          cache: false

      - name: Restore source downloads and compiler cache
        uses: actions/cache@v4
        with:
          path: |
            dl
            .ccache
          key: rax3000m-nand-mt7981-${{ runner.os }}-${{ hashFiles('toolchain/**', 'target/linux/mediatek/**', '.github/workflows/RAX3000M-NAND-23.05.yml') }}
          restore-keys: |
            rax3000m-nand-mt7981-${{ runner.os }}-

      - name: Add application feeds
        run: |
          cat >> feeds.conf.default <<'FEEDS'
          src-git istore https://github.com/linkease/istore.git;main
          src-git nas_packages https://github.com/linkease/nas-packages.git;master
          src-git nas_luci https://github.com/linkease/nas-packages-luci.git;main
          FEEDS
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          ./scripts/feeds install -d y -p istore luci-app-store
          ./scripts/feeds install -d y -p nas_packages quickstart
          ./scripts/feeds install -d y -p nas_luci luci-app-quickstart

          # This fork's GCC 13 fortify headers trigger libxcrypt's upstream
          # format-nonliteral warning. libxcrypt promotes every warning to an
          # error, so downgrade only this known warning.
          LIBXCRYPT_MAKEFILE="feeds/packages/libs/libxcrypt/Makefile"
          test -f "$LIBXCRYPT_MAKEFILE"
          sed -i '/^include \$(INCLUDE_DIR)\/package.mk$/i TARGET_CFLAGS += -Wno-error=format-nonliteral' "$LIBXCRYPT_MAKEFILE"
          grep -q 'TARGET_CFLAGS += -Wno-error=format-nonliteral' "$LIBXCRYPT_MAKEFILE"

      - name: Add pinned proxy and UI sources
        run: |
          rm -rf package/luci-app-passwall package/luci-app-passwall2 package/passwall-packages
          git clone --depth=1 --branch "$PASSWALL_TAG" https://github.com/Openwrt-Passwall/openwrt-passwall.git /tmp/passwall
          mv /tmp/passwall/luci-app-passwall package/
          git clone --depth=1 --branch "$PASSWALL2_TAG" https://github.com/Openwrt-Passwall/openwrt-passwall2.git /tmp/passwall2
          mv /tmp/passwall2/luci-app-passwall2 package/
          git clone https://github.com/Openwrt-Passwall/openwrt-passwall-packages.git package/passwall-packages
          git -C package/passwall-packages checkout "$PASSWALL_PACKAGES_COMMIT"
          rm -rf package/passwall-packages/.git
          rm -rf package/homeproxy package/luci-app-homeproxy feeds/luci/applications/luci-app-homeproxy package/feeds/luci/luci-app-homeproxy
          git clone https://github.com/immortalwrt/homeproxy.git /tmp/homeproxy
          git -C /tmp/homeproxy checkout "$HOME_PROXY_COMMIT"
          rm -rf /tmp/homeproxy/.git
          cp -a /tmp/homeproxy package/homeproxy
          rm -rf package/luci-theme-argon package/luci-app-argon-config
          git clone --depth=1 https://github.com/jerrykuku/luci-theme-argon.git package/luci-theme-argon
          git clone --depth=1 https://github.com/jerrykuku/luci-app-argon-config.git package/luci-app-argon-config
          rm -rf package/luci-app-openclash
          git clone --depth=1 https://github.com/vernesong/OpenClash.git /tmp/OpenClash
          cp -a /tmp/OpenClash/luci-app-openclash package/luci-app-openclash

      - name: Add first boot settings
        run: |
          ROOT_HASH="$(openssl passwd -1 'password')"
          sed -i "s#^root:[^:]*:#root:${ROOT_HASH}:#" package/base-files/files/etc/shadow
          mkdir -p files/etc
          cp package/base-files/files/etc/shadow files/etc/shadow
          printf '%s\n' "$ROOT_HASH" > files/etc/dulwifi-root.hash
          chmod 600 files/etc/shadow files/etc/dulwifi-root.hash
          grep -q '^root:\$1\$' files/etc/shadow
          mkdir -p package/base-files/files/etc/uci-defaults package/base-files/files/usr/libexec package/base-files/files/etc/init.d package/base-files/files/etc/hotplug.d/iface
          cat > package/base-files/files/etc/uci-defaults/90-dulwifi-defaults <<'DEFAULTS'
          #!/bin/sh
          uci -q set network.lan.ipaddr='192.168.9.1'
          uci -q commit network
          uci -q set system.@system[0].hostname='DulWiFi-Tk'
          uci -q commit system
          HASH="$(cat /etc/dulwifi-root.hash)"
          [ -n "$HASH" ] || exit 1
          sed -i "s#^root:[^:]*:#root:${HASH}:#" /etc/shadow
          chmod 600 /etc/shadow
          rm -f /etc/dulwifi-root.hash
          n=0
          for s in $(uci -q show wireless | sed -n "s/^wireless\.\([^=]*\)=wifi-iface$/\1/p"); do
            n=$((n+1))
            [ "$n" -eq 1 ] && ssid='DulWiFi-2.4G'
            [ "$n" -eq 2 ] && ssid='DulWiFi-5G'
            [ "$n" -gt 2 ] && continue
            uci -q set wireless.$s.ssid="$ssid"
            uci -q set wireless.$s.encryption='psk2'
            uci -q set wireless.$s.key='password'
            uci -q set wireless.$s.disabled='0'
          done
          uci -q commit wireless
          exit 0
          DEFAULTS
          cat > package/base-files/files/usr/libexec/nps-wan-mac <<'NPS'
          #!/bin/sh
          [ -e /etc/nps-wan-mac.done ] && exit 0
          [ -f /etc/config/nps ] && [ -x /etc/init.d/nps ] || exit 1
          valid_mac() { case "$1" in ''|00:00:00:00:00:00) return 1;; ??:??:??:??:??:??) return 0;; *) return 1;; esac; }
          get_mac() {
            mac="$(uci -q get network.wan.macaddr)"; valid_mac "$mac" && return 0
            dev="$(uci -q get network.wan.device)"; dev="${dev#@}"
            [ -n "$dev" ] || dev="$(ip route 2>/dev/null | awk '/default/ {print $5; exit}')"
            [ -n "$dev" ] && [ -r "/sys/class/net/$dev/address" ] && mac="$(cat "/sys/class/net/$dev/address")"
            valid_mac "$mac" && return 0
            for lower in /sys/class/net/"$dev"/lower_*; do
              [ -r "$lower/address" ] || continue
              mac="$(cat "$lower/address")"; valid_mac "$mac" && return 0
            done
            return 1
          }
          tries=0
          while [ "$tries" -lt 60 ]; do get_mac && break; tries=$((tries+1)); sleep 2; done
          valid_mac "$mac" || exit 1
          key="$(printf %s "$mac" | tr -d ':' | tr '[:lower:]' '[:upper:]')"
          uci -q set nps.@nps[0].enabled='1'
          uci -q set nps.@nps[0].server_addr='8.134.201.189'
          uci -q set nps.@nps[0].server_port='8024'
          uci -q set nps.@nps[0].protocol='tcp'
          uci -q set nps.@nps[0].compress='1'
          uci -q set nps.@nps[0].crypt='1'
          uci -q set nps.@nps[0].vkey="$key"
          uci -q commit nps
          /etc/init.d/nps enable
          /etc/init.d/nps restart
          touch /etc/nps-wan-mac.done
          NPS
          cat > package/base-files/files/etc/init.d/nps-wan-mac <<'INIT'
          #!/bin/sh /etc/rc.common
          START=98
          start() { [ -e /etc/nps-wan-mac.done ] || /usr/libexec/nps-wan-mac >/dev/null 2>&1 & }
          INIT
          cat > package/base-files/files/etc/hotplug.d/iface/99-nps-wan-mac <<'HOTPLUG'
          #!/bin/sh
          [ "$INTERFACE" = wan ] && [ "$ACTION" = ifup ] && [ ! -e /etc/nps-wan-mac.done ] && /usr/libexec/nps-wan-mac >/dev/null 2>&1 &
          HOTPLUG
          cat > package/base-files/files/etc/uci-defaults/99-enable-nps-wan-mac <<'ENABLE'
          #!/bin/sh
          /etc/init.d/nps-wan-mac enable
          /etc/init.d/nps-wan-mac start
          exit 0
          ENABLE
          chmod 0755 package/base-files/files/etc/uci-defaults/90-dulwifi-defaults package/base-files/files/usr/libexec/nps-wan-mac package/base-files/files/etc/init.d/nps-wan-mac package/base-files/files/etc/hotplug.d/iface/99-nps-wan-mac package/base-files/files/etc/uci-defaults/99-enable-nps-wan-mac

      - name: Select RAX3000M NAND with MTK proprietary WiFi and full packages
        run: |
          # Seed the source tree's MT7981 vendor WiFi feature switches, then select
          # only the RAX3000M NAND MTK-layout target and requested packages.
          # This target already uses the dedicated MTK-layout DTS.  The vendor
          # driver feature switches come from the source defconfig below.
          test -f defconfig/mt7981-ax3000.config
          cp defconfig/mt7981-ax3000.config .config
          sed -i '/^CONFIG_TARGET_DEVICE_mediatek_mt7981_DEVICE_.*=y$/d' .config
          cat >> .config <<'CONFIG'
          CONFIG_TARGET_mediatek=y
          CONFIG_TARGET_mediatek_mt7981=y
          CONFIG_TARGET_DEVICE_mediatek_mt7981_DEVICE_cmcc_rax3000m=y
          CONFIG_CCACHE=y
          CONFIG_PACKAGE_luci=y
          CONFIG_LUCI_LANG_zh_Hans=y
          CONFIG_PACKAGE_kmod-conninfra=y
          CONFIG_PACKAGE_kmod-mt_wifi=y
          CONFIG_PACKAGE_kmod-warp=y
          CONFIG_PACKAGE_mtwifi-cfg=y
          CONFIG_PACKAGE_luci-app-mtwifi-cfg=y
          CONFIG_PACKAGE_luci-app-store=y
          CONFIG_PACKAGE_luci-app-quickstart=y
          CONFIG_PACKAGE_luci-theme-argon=y
          CONFIG_PACKAGE_luci-app-argon-config=y
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-passwall=y
          CONFIG_PACKAGE_luci-app-passwall2=y
          CONFIG_PACKAGE_luci-app-homeproxy=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-nps=y
          CONFIG_PACKAGE_npc=y
          CONFIG_PACKAGE_luci-app-advanced-reboot=y
          CONFIG_PACKAGE_luci-app-watchcat=y
          CONFIG_PACKAGE_dnsmasq=n
          CONFIG_PACKAGE_dnsmasq-full=y
          # CONFIG_PACKAGE_kmod-mt7915e is not set
          # CONFIG_PACKAGE_kmod-mt7981-firmware is not set
          # CONFIG_PACKAGE_mt7981-wo-firmware is not set
          CONFIG

          make defconfig
          grep -q '^CONFIG_TARGET_DEVICE_mediatek_mt7981_DEVICE_cmcc_rax3000m=y$' .config
          for p in kmod-conninfra kmod-mt_wifi kmod-warp mtwifi-cfg luci-app-mtwifi-cfg luci-app-store luci-app-quickstart luci-theme-argon luci-app-ttyd luci-app-passwall luci-app-passwall2 luci-app-homeproxy luci-app-openclash luci-app-nps npc; do
            grep -q "^CONFIG_PACKAGE_$p=y$" .config || { echo "Missing $p"; exit 1; }
          done
          ! grep -q '^CONFIG_PACKAGE_kmod-mt7915e=y$' .config
          ./scripts/diffconfig.sh | tee build.config

      - name: Download and compile
        run: |
          mkdir -p "$CCACHE_DIR"
          ccache --max-size=5G
          make download -j"$(nproc)"
          rm -rf dl/go-mod-cache
          make -j"$(nproc)" || make -j1 V=s
          ccache --show-stats || true

      - name: Verify and collect
        run: |
          ROOTFS="$(find build_dir -path '*/root-mediatek/etc/shadow' -type f | head -n1)"
          test -n "$ROOTFS"
          EXPECTED_HASH="$(cut -d: -f2 files/etc/shadow | head -n1)"
          ACTUAL_HASH="$(awk -F: '$1 == "root" {print $2}' "$ROOTFS")"
          test -n "$EXPECTED_HASH"
          if [ "$ACTUAL_HASH" != "$EXPECTED_HASH" ]; then
            echo 'ERROR: final root password hash differs from configured password'
            exit 1
          fi
          echo 'Final root password hash verified; additional system accounts are allowed.'
          ROOTDIR="${ROOTFS%/etc/shadow}"
          grep -q "hostname='DulWiFi-Tk'" "$ROOTDIR/etc/uci-defaults/90-dulwifi-defaults"
          grep -q '192.168.9.1' "$ROOTDIR/etc/uci-defaults/90-dulwifi-defaults"
          grep -q '8.134.201.189' "$ROOTDIR/usr/libexec/nps-wan-mac"
          mkdir -p release
          IMAGE=$(find bin/targets/mediatek/filogic -type f -name "*cmcc_rax3000m*sysupgrade.bin" -size +5M | head -n1)
          MANIFEST=$(find bin/targets/mediatek/filogic -type f -name "*cmcc_rax3000m*.manifest" | head -n1)
          test -n "$IMAGE"
          test -n "$MANIFEST"
          for p in kmod-mt_wifi luci-app-store luci-app-passwall luci-app-passwall2 luci-app-homeproxy luci-app-openclash luci-app-nps npc; do grep -q "^$p " "$MANIFEST" || exit 1; done
          cp "$IMAGE" "$MANIFEST" build.config release/
          # The MTK-layout device profile emits the sysupgrade image directly;
          # no prebuilt firmware or foreign bootloader is imported.
          sha256sum release/* | tee release/SHA256SUMS
      - uses: actions/upload-artifact@v4
        with:
          name: RAX3000M-NAND-DulWiFi-${{ github.run_number }}
          path: release/
          if-no-files-found: error
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="RAX3000M-NAND-DulWiFi-${GITHUB_RUN_NUMBER}"
          gh release create "$TAG" release/* --target "${GITHUB_SHA}" --title "RAX3000M NAND DulWiFi ${GITHUB_RUN_NUMBER}" --notes "Hostname DulWiFi-Tk; CMCC RAX3000M SPI-NAND with the source MTK layout DTS; 237 source MTK proprietary WiFi; 192.168.9.1/password; DulWiFi 2.4G and 5G; PassWall 26.3.6-1; PassWall2 26.3.5-1; HomeProxy, OpenClash, iStore, QuickStart, Argon, TTYD and feed NPS. NPS uses first-boot physical WAN MAC key and 8.134.201.189:8024."
